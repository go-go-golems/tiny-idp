apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: tinyidp-jitsi
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  vaultConnectionRef: vault
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: tinyidp-jitsi
    serviceAccount: tinyidp-jitsi
